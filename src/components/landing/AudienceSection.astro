---
import { audience, audienceNot } from "@/data/site";
---

<section class="section split-section">
  <div class="split-copy reveal">
    <p class="eyebrow">Для кого</p>
    <h2>Для тех, кто хочет трезвый план, а не мотивационный плакат</h2>
  </div>
  <div class="check-list">
    {audience.map((item) => (
      <div class="check-item reveal">
        <span>✓</span>
        <p>{item}</p>
      </div>
    ))}
    {audienceNot.map((item) => (
      <div class="check-item reveal is-negative">
        <span>✕</span>
        <p>{item}</p>
      </div>
    ))}
  </div>
</section>
